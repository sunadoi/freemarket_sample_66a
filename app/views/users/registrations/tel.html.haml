%h2 電話番番号を入力してください
= form_with(model: @user, url: tel_path, local: true) do |form|
  .admin-card__form__coupon
    .admin-card__form__coupon__input
      = form.label :tel, "TEL", class: "label"
      = form.text_field :tel, required: true, class: "form-input description"
  .admin-card__form__submit
    = form.submit "次へ",  class: "form-submit btn btn-primary"